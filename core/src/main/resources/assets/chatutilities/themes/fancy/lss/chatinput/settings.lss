@import super();

.content {
  .setting-header {
    top: 0;

    .button {
      min-width: 12;
      max-width: 12;

      Icon {

      }
    }

    .back-button {
      Icon {

        &:hover {

        }
      }
    }

    .add-button {
      Icon {
        &:hover {
        }
      }
    }

    .title {

    }
  }

  .scroll {
    margin-bottom: 0;

    .list {
      Setting {
        min-height: 20;
        max-height: 20;

        .setting-content {
          max-height: 20;
          padding-right: 1;
        }

        .header {
          min-height: 20 !important;
          max-height: 20 !important;
          height: 20 !important;

          .info {
            min-height: 20 !important;
            max-height: 20 !important;
            height: 20 !important;

            .status-indicator {
              height: 20;
            }

            Component {

            }
          }
        }

        .button {
          &:hover {

          }
        }

        .input-wrapper {
          TextField {
            &:focus {
            }
          }

          Switch {
            renderer: "Switch";
            width: 30;
          }

          Slider {
            background-color: rgba(0, 0, 0, 0);
            filter: blur(0);
            margin: 0 2 0 2;
          }

          ColorPicker {
          }
        }
      }

    }

    Scrollbar {

    }
  }
}